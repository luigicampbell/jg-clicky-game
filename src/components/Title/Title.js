import React from "react";
import "./title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Jeff Goldblum Clicky Game!</h1>
        <h2>Click on an image to earn points, but don't click on any more than once!</h2>
    </div>
);

export default Title;
